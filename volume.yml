version: '3'
services:
    nginx:
        volums:
            - /mnt/scratch/tmp/nostr/nginx/fullchain.pem:/etc/veryprivate/fullchain.pem
            - /mnt/scratch/tmp/nostr/nginx/privkey.pem:/etc/veryprivate/privkey.pem
    relay:
        volumes:
            - /mnt/scratch/tmp/nostr/config.toml:/usr/src/app/config.toml
            - /mnt/scratch/tmp/nostr/data:/usr/src/app/db