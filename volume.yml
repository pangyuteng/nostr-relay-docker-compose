version: '3'
services:
    nginx:
        volumes:
            - /etc/letsencrypt/live/nostr.aigonewrong.com/fullchain.pem
            - /etc/letsencrypt/live/nostr.aigonewrong.com/privkey.pem
    relay:
        volumes:
            - /mnt/scratch/tmp/nostr/config.toml:/usr/src/app/config.toml
            - /mnt/scratch/tmp/nostr/data:/usr/src/app/db